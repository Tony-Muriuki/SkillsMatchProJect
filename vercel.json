{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist/skillmatch-ai/browser",
  "framework": "angular",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
